#!/bin/bash

sleep 1

# no-gui apps
swaymsg 'exec copyq'

swaymsg 'workspace 2; exec alacritty' && sleep 1
swaymsg 'exec spotify --ozone-platform=wayland --enable-features=UseOzonePlatform,WaylandwindowDecorators,WebRTCPipeWireCapturer' && sleep 1
swaymsg 'exec thunderbird' && sleep 1
